runs:
  using: "composite"
  steps:
    - name: Setup python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9
    - name: Pip install
      shell: bash
      run: |
        pip3 install -r requirements.txt
